#assembly "RevTest"
#class public StringUtil
  #func public static string Rev, string s
    dim string result
    result = ""
    dim int i
    i = s.Length - 1
    while i >= 0
      result = result + s.Substring(i, 1)
      i -= 1
    wend
    return result
  #endfunc
#endclass
